buildscript {
    repositories {
        jcenter()
    }
    dependencies {
    }
}

allprojects {
    ext {
        publish_version = '1.3.0'

        kotlin_version = '1.2.31'

        junit = '4.12'

        bintray_release = '0.7.0'
    }

    repositories {
        jcenter()
    }
}

task clean(type: Delete) {
    delete rootProject.buildDir
}
